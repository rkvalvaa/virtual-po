# CCT-2076 request archiving

Approved design: ../specs/2026-09-09-cct-2058-remaining-design.md.

1. Add reversible archive metadata, retaining workflow status and related records.
2. Verify current membership, owner-draft/reviewer permissions, serialized request locking, and active run/export rejection. Bulk operations report each result.
3. Exclude archived requests from active searches, counts, analytics, planning, agent context/admission, and export admission. Preserve direct access to history.
4. Add an archived view and accessible individual/bulk archive/restore controls, with partial failure feedback.
5. Verify authorization, archive/restore preservation, active work races, query exclusions, and browser behavior on disposable PostgreSQL; run integration checks before delivery.

Local evidence: archive/query/approval regressions passed (33 tests across four files), and browser archive/bulk-restore behavior passed. Bulk partial-failure feedback is covered alongside the existing table suite (21 tests). Independent review and final aggregate checks remain pending.

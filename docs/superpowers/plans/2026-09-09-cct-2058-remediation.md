# CCT-2058 high-priority remediation

Objective: solve all eight High-priority children of CCT-2058. Linear acceptance criteria remain authoritative; this checklist does not narrow them. Other children of the parent remain outside this task.

Use existing application patterns, test first, and verify with the disposable `vpo-audit-pg` database at localhost:55435/vpo_audit. Provider calls are mocked in tests. Preserve existing untracked documents and skills.

- CCT-2059: authorize requests before reading export content; scope content reads and external-key writes by request/organization; validate destinations; test foreign IDs and authorized exports for Linear/Jira/GitHub.
- CCT-2060: admin name editor with save/cancel/validation, session-derived organization, current membership check, audit history, persisted reload. Keep slug immutable and explain it.
- CCT-2061: role-specific email invitations, pending list, resend/revoke, hashed expiring single-use tokens, authenticated matching identity, existing/new accounts, idempotent acceptance, preserved memberships, honest delivery state. Test expired/revoked/wrong-recipient/foreign-tenant paths.
- CCT-2062: confirmed role/removal controls and explicit handover, audit trail, transactionally protected last administrator, immediate permissions, assignment policy. Test simultaneous demotion/removal and revoked access.
- CCT-2064: canonical validated framework/weights/thresholds, reconcile legacy configuration, consistent assessment and labels, finite ranges/totals/ordered thresholds, versioned policy snapshots and explicit historical recalculation.
- CCT-2065: request and epic/story editor including criteria/estimates/order/add/remove, save/cancel/validation, revisions/conflict detection, lifecycle rules and renewed approval, accepted human text exported and protected against AI overwrite.
- CCT-2066: server-priority-sorted complete status set, stable pagination/true totals, queue-scoped search/filter, page-scoped bulk controls. Test 51+ rows and mixed-status boundaries.
- CCT-2067: durable parent/story export state, complete/partial/failed feedback, retries and updates, concurrency and ambiguous provider-success recovery, full story content. Build on tenant fix; test failure/resume/repeat behavior.

## Evidence and remaining work

- CCT-2059: export authorization and direct scoped-read/write regressions pass for all three providers. Migration 0040 corrects a schema constraint that rejected GITHUB_ISSUES integrations.
- CCT-2060: save/cancel/validation/current-admin checks and audit persistence pass database/component tests; browser verifies renamed organization after reload.
- CCT-2061: hashed tokens, rotation, expiry/revocation, matching database identity, idempotent acceptance, delivery states, and membership-preserving session switch are implemented. Database/component/email tests pass; browser verifies joining the destination as Reviewer while retaining the original membership. Delivery requires RESEND_API_KEY, EMAIL_FROM, and AUTH_URL (or NEXT_PUBLIC_APP_URL); missing setup is disclosed and sending is disabled.
- CCT-2062: confirmed role/remove/handover controls and serialized last-admin checks pass. Removed users lose access; assigned requests are unassigned and authorship retained. Named approval steps must be reassigned before removing review access; step assignment shares the organization lock and validates current membership. Existing browser demotion/revocation checks pass.
- CCT-2064: validated canonical versioned policy, server-calculated RICE/WSJF/Custom scores, assessment snapshots, policy-aware badges and analytics, and explicit reassessment history are implemented. Mixed historical threshold regression passes; browser saves and reloads policy version 1.
- CCT-2065: title/summary/epic/story editor includes criteria, notes, estimates, ordering, add/remove, save/cancel, revision history, and optimistic conflicts. Material changes reset review or assessment. Intake preserves explicitly revised summaries; output generation cannot overwrite refined or exported content. Browser verifies saved fields and a rejected stale edit.
- CCT-2066: full queue status sets, priority ordering before pagination, deterministic ties, true totals, scoped search/filter, and page selection reset pass 53-row database/browser fixtures.
- CCT-2067: durable export manifests hold parent/story progress and immutable payloads. A lease prevents concurrent creates; unknown outcomes reconcile via Linear UUIDs, Jira exact labels, or GitHub body references. Definitive failures retry, successful items are reused, and link failures resume. All providers preserve criteria/notes/estimates/order in body text. UI includes GitHub and exposes partial progress/errors after parent creation. The documented lifecycle freezes local content once export starts; subsequent content changes belong in the external tracker. Retry does not overwrite remote edits. Ambiguous outcomes that cannot be located remain unknown instead of risking duplicates.
- Migrations 0040–0044 have been applied only to the disposable database. Apply them before rolling out this application build; preserve export/revision data on rollback (roll back application code rather than destructive down migrations).
- Independent read-only review found three integration gaps (human summary overwrite, fixed analytics thresholds, non-retryable Linear validation failures). Each was reproduced with a failing regression and fixed. A browser-discovered scoring selector overflow at 200% text size was fixed and the full browser suite rerun.
- Verification: 897 tests in 95 files passed with real local PostgreSQL; lint, TypeScript, and production build passed. The full 24-test Playwright suite passed, including 390/768/1366px layouts at enlarged text size. The expanded five-test remediation suite then passed, additionally verifying invitation acceptance on a first login with automatic personal-workspace provisioning (25 distinct browser tests total). Provider exports and email delivery were mocked, not sent live. Scoped re-review confirmed all three review findings closed.
- A fresh npm audit identified low-severity GHSA-866g-f22w-33x8 in the AI SDK dependency chain. Compatible ai 6.0.278 / @ai-sdk/react 3.0.281 / @ai-sdk/anthropic 3.0.116 patches restore a zero-vulnerability audit. All 897 tests and the complete 25-test local browser suite passed with the patched SDK. Linux CI additionally caught a long refinement-link overflow at enlarged text size; the link now wraps within available width.
- Typecheck initially discovered an extracted Playwright trace source under ignored test-results. Exclude generated test-results/playwright-report artifacts from TypeScript inputs; application and test sources remain checked.
- Final integration/CI and production rollout remain. No production migration, deployment, live email, or live tracker export has occurred.
